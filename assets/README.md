# Assets

## Purpose

The assets directory is used to store mission specific files which can then be
copied into a generic image to customize it for the mission while keeping the
baseline image simple and universal.

Each mission adds it's own subdirectory to the assets repo.
